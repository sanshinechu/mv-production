# 工作日誌 2026-06-04

## ✅ 今天完成

### Vertex AI Imagen 生圖 API 串接

- 建立 `scripts/test_vertex_imagen.py`，透過 Service Account JSON 取得 Bearer token，直接呼叫 Vertex AI REST API 生圖
- 解決模型版本問題：
  - `imagegeneration@006` → 已 EOL 停用
  - `imagen-4.0-generate-preview-06-06` → 需要 Preview 存取申請
  - **最終使用 `imagen-3.0-generate-002` 正式版，成功生圖 ✅**
- 生成圖片存放於 `outputs/vertex_test/`

### Imagen CLI 工具（imagen.py）

- 建立 `scripts/imagen.py`，整合至 MV 製作 Step 7
- 支援單張生圖：`python scripts/imagen.py "prompt" --ratio 16:9 --count 2`
- 支援批次生圖：`python scripts/imagen.py --batch scripts/batch_example.yaml`
- 建立 `scripts/batch_example.yaml` 範例批次清單（4 個 MV 場景）
- 加入 429 配額限制自動 retry 機制（等待 65 秒，最多重試 3 次）
- 批次測試結果：3/4 場景成功，共生成 5 張圖

## 🔑 關鍵設定

```
GOOGLE_APPLICATION_CREDENTIALS = I:\我的雲端硬碟\Gemini Gems\glassy-keyword-498400-f7-c38ab260db5f.json
GOOGLE_CLOUD_PROJECT            = glassy-keyword-498400-f7
GOOGLE_CLOUD_LOCATION           = us-central1
MODEL                           = imagen-3.0-generate-002
```

## 🕳️ 踩坑筆記

| 問題 | 原因 | 解法 |
|------|------|------|
| `imagegeneration@006` 404 | 模型已 EOL | 換 `imagen-3.0-generate-002` |
| `imagen-4.0-generate-preview-06-06` 404 | 專案未申請 Preview 存取 | 使用正式版；如需 Imagen 4 需另外申請 |
| PowerShell 執行 Bash 語法失敗 | `$env:VAR=val` 是 PowerShell 語法，不能用 Bash tool | 改用 PowerShell tool |
| 批次生圖 429 配額限制 | Vertex AI 每分鐘請求數上限 | 已加入 auto retry（等 65 秒），與場景間隔 2 秒 |
| Google Drive I:\ 鎖定 | GDrive 同步中，無法寫入 | 等綠勾後再操作；暫存桌面再移過去 |

### 愛情咖啡廳 MV — 正式場景生圖

- 建立 `scripts/mv_love_scenes.yaml`，對應 MV 6 個段落（Verse1 / Pre-Chorus / Chorus / Verse2 / Bridge / Outro）
- 修正 `scripts/imagen.py` Windows 編碼問題（cp950 → UTF-8，移除 emoji 輸出）
- 批次生成 **12 張場景圖**（每場景 2 張），全部成功 ✅
  - 輸出位置：`outputs/mv_scenes/`
  - 觸發 2 次 429 配額限流，auto retry 自動處理，總耗時 ~3.5 分鐘
- **Vertex AI Imagen 配額上限說明**：
  - Service Account 無 Service Usage 讀取權限，無法透過 API 直接查詢
  - 手動查詢路徑：GCP Console → IAM → Quotas → 篩選 `aiplatform.googleapis.com` → 搜尋 `imagen`
  - 目前觀察：每分鐘約可生成 4-6 張，超過就 429，等 65 秒可恢復

### 工作日誌補寫

- 確認今日 git 歷程，補寫工作日誌（本次會話）
- 今日兩次 commit 已 push，GitHub 同步完成 ✅

## ➡️ 下一步

- Step 5：從 `outputs/mv_scenes/` 選出最佳圖放大提質（Upscayl 或 Topaz）
- Step 6：為各場景補充影片動態提示詞，準備送 Kling/Hailuo 生影片
- （可選）至 GCP Console 申請提高 Imagen 每分鐘配額

---

## 🔭 追蹤事項（2026-06-16 補記）

### Imagen 3 → Gemini 3.1 Flash Image 遷移（目前不急，待 Google 公布退場日）

**緣由：** 收到 Google 來信通知 **Imagen 4**（`imagen-4.0-*`）於 **2026-08-17** 停用。
盤點後確認本專案三個生圖腳本用的是 **Imagen 3**，不在那封信的範圍內：

| 腳本 | 模型 | 專案 ID |
|------|------|---------|
| `scripts/imagen.py`（主力 CLI / 批次） | `imagen-3.0-generate-002` | `glassy-keyword-498400-f7` |
| `scripts/test_vertex_imagen.py` | `imagen-3.0-generate-002` | `glassy-keyword-498400-f7` |
| `scripts/vertex_imagen.py` | `imagen-3.0-generate-001` | `gen-lang-client-0086054341` |

**現況判定（為什麼不急）：**
- 官方 Vertex AI deprecations 頁面**尚未公布 Imagen 3 模型端點的退場日**。
- 網路盛傳「所有 Imagen 2026-06-24 停用」是把 **Vertex AI SDK 的 generative 模組**退場（那才是 6/24）跟模型退場混淆了。本專案腳本是**直接打 REST `:predict`**，沒用到那個 SDK 模組，所以 6/24 不影響。
- 官方政策：下一代穩定版上市後才公布退場日，並**提前一個月**停止新接入 → 屆時再遷移仍來得及。

**觸發遷移的條件：** 官方一旦公布 `imagen-3.0-*` 的退場日，就啟動遷移。

**遷移時要做的事：**
1. 三個腳本由 REST `:predict` 改成 Gemini 的 `:generateContent`（payload 結構不同），或改用 `google-genai` SDK（參考剪片專案 `03_剪片工作流/skills/cover-image/draw_gemini.py` 的寫法）。
2. 模型換成 `gemini-3.1-flash-image`。**Vertex 區域要用 `global`**（剪片實測：`us-central1` 會回 404）。
3. 順手統一專案 ID：`vertex_imagen.py` 目前寫死 `gen-lang-client-0086054341`，與另兩個的 `glassy-keyword-498400-f7` 不一致，混用易踩配額/權限。

**備註：** MV Step 7 主力生圖其實是 FLUX.1 Dev（見 `media-generation` skill），Imagen 只是備選路線，遷移優先度低。
